/* eslint-disable no-unused-vars */
export const SELECTORS = { INPUT_ERROR_LIST: 'input-error__list' };

export const PATH = { BASEURL: 'https://ya-praktikum.tech/api/v2' };

export const HEADERS = { JSON: { 'Content-Type': 'application/json' } };

export const DEFAULT_TITLE = 'Easy Touch';

// eslint-disable-next-line no-shadow
export enum StoreEvents {
  updated = 'updated',
}
